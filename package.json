{
  "name": "job-system",
  "version": "1.0.0",
  "description": "A deterministic, modular job search pipeline for DevOps/Cloud/SRE roles.",
  "main": "index.js",
  "scripts": {
    "build": "tsc --outDir dist",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && node dist/src/main.js",
    "debug": "npm run build && node dist/src/scraper/debug.js",
    "scrape": "npm run build && node dist/src/scraper/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.9.1",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "playwright": "^1.60.0"
  }
}
